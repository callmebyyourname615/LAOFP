spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

switching:
  payment:
    json-initiation:
      enabled: ${JSON_INITIATION_ENABLED:true}   # default ON in dev — override to false in .env if needed
