# Dependency Degraded Mode

Every dependency must define timeout, bounded retry, circuit breaker, fallback, backlog limit, recovery procedure and reconciliation requirements.

Manual overrides require two approvers, an incident and a 60-minute maximum duration. Fail closed when DNS, time synchronization or secret verification cannot meet safety requirements.
