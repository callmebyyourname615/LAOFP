# 66B — UAT Dependency Matrix

Populate environment variables referenced by `config/phase66/uat-dependencies.yaml`. Do not place credentials in the YAML or evidence.

## Evidence

Store runtime artifacts under `build/phase66-evidence/<run-id>/66B/`. Redact secrets before signing.
