# 66E — Performance and Capacity Certification

Full mode requires the exact load confirmation string. Thresholds are strict; P95 510 ms fails the 10K gate.

## Evidence

Store runtime artifacts under `build/phase66-evidence/<run-id>/66E/`. Redact secrets before signing.
