# 66H — Secret Rotation Ceremony

Evidence must contain booleans, timestamps, approver identities and ticket references only. Never record generated passwords, Vault values, tokens or private keys.

## Evidence

Store runtime artifacts under `build/phase66-evidence/<run-id>/66H/`. Redact secrets before signing.
