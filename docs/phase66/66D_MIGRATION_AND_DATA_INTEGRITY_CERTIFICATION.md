# 66D — Migration and Data Integrity Certification

The repository inventory rejects duplicate migration versions. Full mode additionally runs read-only SQL against the confirmed UAT database.

## Evidence

Store runtime artifacts under `build/phase66-evidence/<run-id>/66D/`. Redact secrets before signing.
