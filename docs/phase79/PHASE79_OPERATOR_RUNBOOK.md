# Phase 79 Operator Runbook

Production actions require `TARGET_ENVIRONMENT=production` and explicit execution flags. Stop immediately on financial mismatch, duplicate posting, security breach, migration inconsistency or unbounded backlog. Never use example attestations.
