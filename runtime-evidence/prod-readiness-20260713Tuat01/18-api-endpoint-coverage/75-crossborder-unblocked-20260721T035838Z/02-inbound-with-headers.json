{"httpStatus":400,"endpoint":"POST /v1/crossborder/inbound/promptpay (all required headers + payload)","response":{"errorCode":"REQ-001","message":"Inbound partner authentication is not configured"},"note":"Route + all validation flow live. Blocked at business config check: PROMPTPAY_INBOUND_API_KEY is empty in UAT .env (per-rail secret not provisioned) — same class of gap as REPORT_LINK_SIGNING_SECRET. To finish e2e, provision partner API keys for each enabled rail."}
