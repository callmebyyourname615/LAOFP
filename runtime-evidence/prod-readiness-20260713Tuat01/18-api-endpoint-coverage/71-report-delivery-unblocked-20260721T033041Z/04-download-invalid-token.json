{"httpStatus":400,"endpoint":"GET /v1/reports/download/{id}?expires=<future>&token=deadbeef","response":{"errorCode":"REQ-001","message":"Report link signing secret is not configured"},"note":"Route registered and validation flow works, but the download feature cannot actually deliver files because REPORT_LINK_SIGNING_SECRET is unset on UAT (defaults to empty in docker-compose)."}
