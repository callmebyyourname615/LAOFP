{"endpoints":["POST /api/operations/settlement/instructions/{ref}/approve","POST .../reject","POST .../send-rtgs","POST .../record-rtgs-upload"],"probe":"PROBE-NONEXISTENT-INSTRUCTION-0001","httpStatus":404,"errorCode":"SET-003","message":"Settlement instruction not found","note":"All 4 mutation endpoints reached SettlementInstructionService and correctly returned 404 for a nonexistent instructionRef. Route + admin auth + business-layer lookup all verified. Full end-to-end happy path (approve real PENDING_APPROVAL instruction) requires actual settled transfers to exist in a cycle, which we do not have today (see 70-settlement-cycle-chain-future-date)."}
