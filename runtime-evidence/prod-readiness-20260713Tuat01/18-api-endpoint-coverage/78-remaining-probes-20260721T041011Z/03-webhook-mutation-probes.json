{"endpoints":{"GET /v1/webhooks/{fakeId}":"404 not found","DELETE /v1/webhooks/{fakeId}":"204 no content (idempotent DELETE — deleting nonexistent id is a no-op success by REST convention)","POST /v1/webhooks/{fakeId}/test":"404 not found","POST /v1/webhooks/{fakeId}/secret/rotate":"400 REQ-001 signingSecret is required (bean validation ran before id lookup)"},"note":"All 4 webhook mutation endpoints registered and reachable via ROLE_BANK (OAuth Bearer). Auth + validation + business-layer id lookup all verified. Full end-to-end happy path (against a real registered webhook) still blocked by the outbound-allowlist config gap documented in 74-bank-role-oauth-unblocked."}
