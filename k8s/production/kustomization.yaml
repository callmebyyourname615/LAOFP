apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - resourcequota.yaml
  - limitrange.yaml
  - default-deny-networkpolicy.yaml
  - allow-ingress-networkpolicy.yaml
  - break-glass-rbac.yaml
commonLabels:
  app.kubernetes.io/part-of: switching
  switching.example.com/environment: production
