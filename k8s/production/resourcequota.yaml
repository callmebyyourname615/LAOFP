apiVersion: v1
kind: ResourceQuota
metadata:
  name: switching-production-quota
  namespace: switching
spec:
  hard:
    requests.cpu: "16"
    requests.memory: 32Gi
    limits.cpu: "32"
    limits.memory: 64Gi
    pods: "40"
    services: "20"
    persistentvolumeclaims: "10"
