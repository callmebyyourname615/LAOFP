apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: phase73-memory-stress-${PHASE73_RUN_ID_SAFE}
  namespace: ${PHASE73_NAMESPACE}
  labels:
    switching.phase: "73H"
    switching.run-id: "${PHASE73_RUN_ID_SAFE}"
spec:
  mode: one
  selector:
    namespaces:
      - ${PHASE73_NAMESPACE}
    labelSelectors:
      app.kubernetes.io/name: ${PHASE73_APP_LABEL}
  stressors:
    memory:
      workers: ${PHASE73_MEMORY_WORKERS}
      size: "${PHASE73_MEMORY_SIZE}"
  duration: "${PHASE73_EXPERIMENT_DURATION}s"
